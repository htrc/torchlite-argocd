apiVersion: v1
kind: Service
metadata:
  name: torchlite-frontend-dev-service
spec:
  selector:
    app: torchlite-frontend-dev-app
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
