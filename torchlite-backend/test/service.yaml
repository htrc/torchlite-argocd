apiVersion: v1
kind: Service
metadata:
  name: torchlite-backend-test-service
spec:
  selector:
    app: torchlite-backend-test-app
  ports:
    - port: 8000
      protocol: TCP
      targetPort: 8000